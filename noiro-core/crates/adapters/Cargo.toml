[package]
name = "noiro-adapters"
description = "Noiro source adapters: normalize Nuvio JS-provider results and Eclipse-music responses onto the one noiro-protocol Stream shape, so non-Stremio-protocol sources run on the same engine (ranking/dedup/resolve never know the difference). Pure data mapping; the JS runtime that executes Nuvio providers is a later phase."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
noiro-protocol = { path = "../protocol" }
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
proptest = "1"
