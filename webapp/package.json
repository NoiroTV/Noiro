{
  "name": "noiro-web-preview",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Archived local Noiro browser preview. Not a deployment target.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/lib/vault.crypto.test.mjs"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "hls.js": "^1.6.16",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^8.0.16"
  }
}
